export interface SingleMessageModel {
	userName: string; // Name of the user
	messageContent: string; // The message content
	isUser: boolean; // Boolean to indicate if the message is from the user
}
